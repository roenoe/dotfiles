# Matches the socket path in ssh-agent-user.service.
# Read by the systemd user manager at login, so it reaches the graphical
# session (GDM imports this environment) and every unit started from it.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
